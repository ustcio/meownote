---
import { getLangFromUrl, useTranslations, type Lang } from '@i18n';
import LanguageSwitcher from '@components/ui/LanguageSwitcher.astro';
import ThemeToggle from '@components/ui/ThemeToggle.astro';
import Icon from '@components/ui/Icon.astro';
import AuthModal from '@components/ui/AuthModal.astro';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

const navItems = [
  { href: '/', label: t('nav.home'), id: 'home' },
  { href: '/research', label: t('nav.research'), id: 'research' },
  { href: '/chatbot', label: t('nav.chatbot'), id: 'chatbot' },
  { href: '/kit', label: t('nav.kit'), id: 'kit' },
  { href: '/about', label: t('nav.about'), id: 'about' },
];

const currentPath = Astro.url.pathname;
---

<header class="header">
  <div class="header-container">
    <a href="/" class="logo">
      <video 
        class="logo-video" 
        autoplay 
        loop 
        muted 
        playsinline 
        preload="auto" 
        poster="/favicon-32x32.png"
      >
        <source src="/logo.mp4" type="video/mp4" />
      </video>
      <span class="logo-text">Meow Note</span>
    </a>
    
    <nav class="nav" aria-label="Main navigation">
      <ul class="nav-list">
        {navItems.map((item) => (
          <li>
            <a 
              href={item.href}
              class:list={['nav-link', { active: currentPath === item.href || (item.href !== '/' && currentPath.startsWith(item.href)) }]}
            >
              {item.label}
            </a>
          </li>
        ))}
      </ul>
    </nav>
    
    <div class="header-actions">
      <ThemeToggle />
      <LanguageSwitcher />
      
      <div class="user-menu" id="user-menu">
        <button class="auth-btn" id="auth-btn" aria-label="Login">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path>
            <circle cx="12" cy="7" r="4"></circle>
          </svg>
          <span class="auth-btn-text">{t('auth.login')}</span>
        </button>
        <div class="user-dropdown" id="user-dropdown">
          <a href="/profile" class="dropdown-item">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path>
              <circle cx="12" cy="7" r="4"></circle>
            </svg>
            <span>{lang === 'zh' ? '个人资料' : 'Profile'}</span>
          </a>
          <button class="dropdown-item logout" id="dropdown-logout">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"></path>
              <polyline points="16 17 21 12 16 7"></polyline>
              <line x1="21" y1="12" x2="9" y2="12"></line>
            </svg>
            <span>{t('auth.logout')}</span>
          </button>
        </div>
      </div>
      
      <button 
        class="mobile-menu-btn" 
        aria-label="Toggle menu" 
        aria-expanded="false"
        aria-controls="mobile-nav"
      >
        <Icon name="menu" size={18} />
      </button>
    </div>
  </div>
  
  <nav id="mobile-nav" class="mobile-nav" aria-label="Mobile navigation">
    <ul class="mobile-nav-list">
      {navItems.map((item) => (
        <li>
          <a 
            href={item.href}
            class:list={['mobile-nav-link', { active: currentPath === item.href || (item.href !== '/' && currentPath.startsWith(item.href)) }]}
          >
            {item.label}
          </a>
        </li>
      ))}
      <li>
        <button class="mobile-auth-btn" id="mobile-auth-btn">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path>
            <circle cx="12" cy="7" r="4"></circle>
          </svg>
          <span>{t('auth.login')}</span>
        </button>
      </li>
    </ul>
  </nav>
</header>

<AuthModal />

<script>
  const mobileMenuBtn = document.querySelector('.mobile-menu-btn');
  const mobileNav = document.getElementById('mobile-nav');
  const authBtn = document.getElementById('auth-btn');
  const mobileAuthBtn = document.getElementById('mobile-auth-btn');
  const userMenu = document.getElementById('user-menu');
  const dropdownLogout = document.getElementById('dropdown-logout');
  
  mobileMenuBtn?.addEventListener('click', () => {
    const isExpanded = mobileMenuBtn.getAttribute('aria-expanded') === 'true';
    mobileMenuBtn.setAttribute('aria-expanded', String(!isExpanded));
    mobileNav?.classList.toggle('open');
  });
  
  document.querySelectorAll('.mobile-nav-link').forEach(link => {
    link.addEventListener('click', () => {
      mobileMenuBtn?.setAttribute('aria-expanded', 'false');
      mobileNav?.classList.remove('open');
    });
  });
  
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') {
      mobileMenuBtn?.setAttribute('aria-expanded', 'false');
      mobileNav?.classList.remove('open');
      userMenu?.classList.remove('open');
    }
  });
  
  function openAuthModal() {
    (window as any).openAuthModal?.('login');
  }
  
  authBtn?.addEventListener('click', (e) => {
    const user = localStorage.getItem('user');
    if (user) {
      e.stopPropagation();
      userMenu?.classList.toggle('open');
    } else {
      openAuthModal();
    }
  });
  
  document.addEventListener('click', (e) => {
    if (!userMenu?.contains(e.target as Node)) {
      userMenu?.classList.remove('open');
    }
  });
  
  mobileAuthBtn?.addEventListener('click', () => {
    const user = localStorage.getItem('user');
    if (user) {
      window.location.href = '/profile';
    } else {
      mobileNav?.classList.remove('open');
      mobileMenuBtn?.setAttribute('aria-expanded', 'false');
      openAuthModal();
    }
  });
  
  dropdownLogout?.addEventListener('click', () => {
    localStorage.removeItem('auth_token');
    localStorage.removeItem('user');
    window.dispatchEvent(new CustomEvent('auth:logout'));
    window.location.reload();
  });
  
  function updateAuthUI() {
    const user = localStorage.getItem('user');
    const authBtnText = document.querySelector('.auth-btn-text');
    const mobileAuthBtnSpan = mobileAuthBtn?.querySelector('span');
    
    if (user) {
      const userData = JSON.parse(user);
      if (authBtnText) authBtnText.textContent = userData.username;
      if (mobileAuthBtnSpan) mobileAuthBtnSpan.textContent = userData.username;
      userMenu?.classList.add('logged-in');
    } else {
      if (authBtnText) authBtnText.textContent = 'Login';
      userMenu?.classList.remove('logged-in');
    }
  }
  
  window.addEventListener('auth:login', updateAuthUI);
  window.addEventListener('auth:logout', updateAuthUI);
  document.addEventListener('DOMContentLoaded', updateAuthUI);
</script>

<style>
  .header {
    position: fixed;
    top: var(--space-4);
    left: var(--space-4);
    right: var(--space-4);
    z-index: var(--z-fixed);
    background: var(--glass-bg);
    backdrop-filter: var(--surface-blur-heavy);
    -webkit-backdrop-filter: var(--surface-blur-heavy);
    border: 1px solid var(--glass-border);
    border-radius: var(--radius-2xl);
    box-shadow: var(--shadow-float);
    transition: background var(--duration-normal) var(--ease-default),
                border-color var(--duration-normal) var(--ease-default),
                box-shadow var(--duration-normal) var(--ease-default);
  }
  
  .header:hover {
    background: var(--glass-bg-hover);
    border-color: var(--glass-border-hover);
  }
  
  @media (min-width: 768px) {
    .header {
      top: var(--space-5);
      left: var(--space-6);
      right: var(--space-6);
    }
  }
  
  @media (min-width: 1024px) {
    .header {
      top: var(--space-6);
      left: var(--space-8);
      right: var(--space-8);
    }
  }
  
  .header-container {
    display: flex;
    align-items: center;
    justify-content: space-between;
    max-width: var(--container-xl);
    margin: 0 auto;
    padding: var(--space-3) var(--space-4);
    height: var(--header-height);
  }
  
  @media (min-width: 768px) {
    .header-container {
      padding: var(--space-3) var(--space-5);
    }
  }
  
  .logo {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    text-decoration: none;
    color: var(--text-primary);
  }
  
  .logo-video {
    width: 32px;
    height: 32px;
    border-radius: var(--radius-md);
    object-fit: cover;
  }
  
  .logo-text {
    display: none;
    font-family: var(--font-brand);
    font-size: var(--text-lg);
    letter-spacing: 0.02em;
  }
  
  @media (min-width: 640px) {
    .logo-text {
      display: block;
    }
  }
  
  .nav {
    display: none;
  }
  
  @media (min-width: 768px) {
    .nav {
      display: block;
    }
  }
  
  .nav-list {
    display: flex;
    align-items: center;
    gap: var(--space-1);
    list-style: none;
  }
  
  .nav-link {
    position: relative;
    display: block;
    padding: var(--space-2) var(--space-4);
    color: var(--text-secondary);
    font-family: var(--font-sans);
    font-size: var(--text-sm);
    font-weight: var(--font-medium);
    text-decoration: none;
    border-radius: var(--radius-lg);
    transition: color var(--duration-fast) var(--ease-default),
                background-color var(--duration-fast) var(--ease-default);
  }
  
  .nav-link:hover {
    color: var(--text-primary);
    background-color: var(--bg-tertiary);
  }
  
  .nav-link.active {
    color: var(--text-primary);
    background-color: var(--bg-tertiary);
  }
  
  .header-actions {
    display: flex;
    align-items: center;
    gap: var(--space-1);
  }
  
  .auth-btn {
    display: none;
    align-items: center;
    gap: var(--space-2);
    padding: var(--space-2) var(--space-3);
    color: var(--text-secondary);
    font-size: var(--text-sm);
    font-weight: var(--font-medium);
    border-radius: var(--radius-lg);
    transition: color var(--duration-fast) var(--ease-default),
                background-color var(--duration-fast) var(--ease-default);
  }
  
  .auth-btn:hover {
    color: var(--text-primary);
    background-color: var(--bg-tertiary);
  }
  
  .user-menu {
    position: relative;
  }
  
  .user-dropdown {
    position: absolute;
    top: 100%;
    right: 0;
    margin-top: var(--space-2);
    min-width: 160px;
    background: var(--card-bg);
    border: 1px solid var(--card-border);
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-float);
    padding: var(--space-2);
    opacity: 0;
    visibility: hidden;
    transform: translateY(-10px);
    transition: all var(--duration-fast) var(--ease-default);
    z-index: var(--z-dropdown);
  }
  
  .user-menu:hover .user-dropdown,
  .user-menu.open .user-dropdown {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
  }
  
  .dropdown-item {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    width: 100%;
    padding: var(--space-2) var(--space-3);
    font-size: var(--text-sm);
    color: var(--text-secondary);
    border-radius: var(--radius-lg);
    transition: all var(--duration-fast) var(--ease-default);
  }
  
  .dropdown-item:hover {
    background: var(--bg-tertiary);
    color: var(--text-primary);
  }
  
  .dropdown-item.logout {
    color: #ef4444;
  }
  
  .dropdown-item.logout:hover {
    background: rgba(239, 68, 68, 0.1);
  }
  
  @media (min-width: 768px) {
    .auth-btn {
      display: flex;
    }
  }
  
  .mobile-auth-btn {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    width: 100%;
    padding: var(--space-3) var(--space-4);
    color: var(--text-secondary);
    font-size: var(--text-sm);
    font-weight: var(--font-medium);
    border-radius: var(--radius-lg);
    transition: color var(--duration-fast) var(--ease-default),
                background-color var(--duration-fast) var(--ease-default);
  }
  
  .mobile-auth-btn:hover {
    color: var(--text-primary);
    background-color: var(--bg-tertiary);
  }
  
  .mobile-menu-btn {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    color: var(--text-secondary);
    border-radius: var(--radius-lg);
    transition: color var(--duration-fast) var(--ease-default),
                background-color var(--duration-fast) var(--ease-default);
  }
  
  .mobile-menu-btn:hover {
    color: var(--text-primary);
    background-color: var(--bg-tertiary);
  }
  
  @media (min-width: 768px) {
    .mobile-menu-btn {
      display: none;
    }
  }
  
  .mobile-nav {
    display: none;
    position: absolute;
    top: calc(100% + var(--space-2));
    left: 0;
    right: 0;
    background: var(--glass-bg);
    backdrop-filter: var(--surface-blur-heavy);
    -webkit-backdrop-filter: var(--surface-blur-heavy);
    border: 1px solid var(--glass-border);
    border-radius: var(--radius-2xl);
    padding: var(--space-3);
    animation: slideDown var(--duration-fast) var(--ease-default);
    box-shadow: var(--shadow-float);
  }
  
  .mobile-nav.open {
    display: block;
  }
  
  @media (min-width: 768px) {
    .mobile-nav {
      display: none !important;
    }
  }
  
  .mobile-nav-list {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: var(--space-1);
  }
  
  .mobile-nav-link {
    display: block;
    padding: var(--space-3) var(--space-4);
    color: var(--text-secondary);
    font-family: var(--font-sans);
    font-weight: var(--font-medium);
    font-size: var(--text-sm);
    text-decoration: none;
    border-radius: var(--radius-lg);
    transition: color var(--duration-fast) var(--ease-default),
                background-color var(--duration-fast) var(--ease-default);
  }
  
  .mobile-nav-link:hover,
  .mobile-nav-link.active {
    color: var(--text-primary);
    background-color: var(--bg-tertiary);
  }
  
  @keyframes slideDown {
    from {
      opacity: 0;
      transform: translateY(-8px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
