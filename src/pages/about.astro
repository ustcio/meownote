---
import Layout from '@layouts/Layout.astro';
import { getLangFromUrl, useTranslations } from '@i18n';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

const features = [
  {
    icon: '🚀',
    title: lang === 'zh' ? '高性能' : 'High Performance',
    description: lang === 'zh' 
      ? '极速响应，毫秒级延迟'
      : 'Lightning fast response with millisecond latency'
  },
  {
    icon: '🔒',
    title: lang === 'zh' ? '安全可靠' : 'Secure & Reliable',
    description: lang === 'zh'
      ? '企业级安全保障，数据加密传输'
      : 'Enterprise-grade security with encrypted data transmission'
  },
  {
    icon: '🧠',
    title: lang === 'zh' ? '智能理解' : 'Intelligent Understanding',
    description: lang === 'zh'
      ? '深度理解上下文，精准响应需求'
      : 'Deep context understanding for precise responses'
  },
  {
    icon: '🌍',
    title: lang === 'zh' ? '全球部署' : 'Global Deployment',
    description: lang === 'zh'
      ? '遍布全球的节点，就近访问'
      : 'Global nodes for nearest access'
  },
];

const team = [
  { name: 'Alex Chen', role: lang === 'zh' ? '创始人 & CEO' : 'Founder & CEO' },
  { name: 'Sarah Kim', role: lang === 'zh' ? '技术总监' : 'CTO' },
  { name: 'Mike Wang', role: lang === 'zh' ? '产品负责人' : 'Head of Product' },
];
---

<Layout 
  title={lang === 'zh' ? '关于 - Meow Note' : 'About - Meow Note'}
  description={t('about.subtitle')}
>
  <section class="about">
    <div class="about-container">
      <div class="about-hero">
        <h1 class="about-title">{t('about.title')}</h1>
        <p class="about-subtitle">{t('about.subtitle')}</p>
      </div>
      
      <div class="about-content">
        <div class="about-section">
          <h2 class="section-title">
            {lang === 'zh' ? '我们的使命' : 'Our Mission'}
          </h2>
          <p class="section-text">
            {lang === 'zh'
              ? '我们致力于构建安全、可靠、易用的AI工具，让每个人都能享受到人工智能带来的便利。'
              : 'We are dedicated to building safe, reliable, and easy-to-use AI tools, making artificial intelligence accessible to everyone.'}
          </p>
        </div>
        
        <div class="features-grid">
          {features.map((feature) => (
            <div class="feature-card hover-lift">
              <span class="feature-icon">{feature.icon}</span>
              <h3 class="feature-title">{feature.title}</h3>
              <p class="feature-description">{feature.description}</p>
            </div>
          ))}
        </div>
        
        <div class="team-section">
          <h2 class="section-title">
            {lang === 'zh' ? '核心团队' : 'Core Team'}
          </h2>
          <div class="team-grid">
            {team.map((member) => (
              <div class="team-card hover-lift">
                <div class="team-avatar">
                  {member.name.split(' ').map(n => n[0]).join('')}
                </div>
                <h3 class="team-name">{member.name}</h3>
                <p class="team-role">{member.role}</p>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  .about {
    min-height: 100vh;
    padding: calc(var(--header-height) + var(--space-8)) var(--space-4) var(--space-12);
  }
  
  .about-container {
    max-width: var(--container-lg);
    margin: 0 auto;
  }
  
  .about-hero {
    text-align: center;
    margin-bottom: var(--space-12);
  }
  
  .about-title {
    font-size: var(--text-4xl);
    font-weight: var(--font-bold);
    color: var(--text-primary);
    margin-bottom: var(--space-4);
  }
  
  @media (min-width: 768px) {
    .about-title {
      font-size: var(--text-5xl);
    }
  }
  
  .about-subtitle {
    color: var(--text-secondary);
    font-size: var(--text-xl);
  }
  
  .about-content {
    display: flex;
    flex-direction: column;
    gap: var(--space-12);
  }
  
  .about-section {
    text-align: center;
    max-width: 600px;
    margin: 0 auto;
  }
  
  .section-title {
    font-size: var(--text-2xl);
    font-weight: var(--font-semibold);
    color: var(--text-primary);
    margin-bottom: var(--space-4);
  }
  
  @media (min-width: 768px) {
    .section-title {
      font-size: var(--text-3xl);
    }
  }
  
  .section-text {
    color: var(--text-secondary);
    font-size: var(--text-lg);
    line-height: var(--leading-relaxed);
  }
  
  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: var(--space-4);
  }
  
  @media (min-width: 768px) {
    .features-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
  
  @media (min-width: 1024px) {
    .features-grid {
      grid-template-columns: repeat(4, 1fr);
    }
  }
  
  .feature-card {
    text-align: center;
    padding: var(--space-6);
    background: var(--card-bg);
    border: 1px solid var(--card-border);
    border-radius: var(--radius-xl);
    box-shadow: var(--card-shadow);
  }
  
  .feature-icon {
    font-size: var(--text-4xl);
    display: block;
    margin-bottom: var(--space-4);
  }
  
  .feature-title {
    font-size: var(--text-lg);
    font-weight: var(--font-semibold);
    color: var(--text-primary);
    margin-bottom: var(--space-2);
  }
  
  .feature-description {
    font-size: var(--text-sm);
    color: var(--text-secondary);
    line-height: var(--leading-relaxed);
  }
  
  .team-section {
    text-align: center;
  }
  
  .team-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: var(--space-6);
    margin-top: var(--space-8);
  }
  
  @media (min-width: 768px) {
    .team-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }
  
  .team-card {
    padding: var(--space-6);
    background: var(--card-bg);
    border: 1px solid var(--card-border);
    border-radius: var(--radius-xl);
    box-shadow: var(--card-shadow);
  }
  
  .team-avatar {
    width: 80px;
    height: 80px;
    margin: 0 auto var(--space-4);
    background: linear-gradient(135deg, var(--color-accent-blue), var(--color-accent-purple));
    border-radius: var(--radius-full);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: var(--text-xl);
    font-weight: var(--font-bold);
    color: white;
  }
  
  .team-name {
    font-size: var(--text-lg);
    font-weight: var(--font-semibold);
    color: var(--text-primary);
    margin-bottom: var(--space-1);
  }
  
  .team-role {
    font-size: var(--text-sm);
    color: var(--text-secondary);
  }
</style>
