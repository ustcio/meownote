---
import Layout from '@layouts/Layout.astro';
import KitToolWrapper from '@components/vue/KitToolWrapper.vue';

const toolName = '即时热榜';
const toolDesc = '实时查看各大平台热搜榜';
---

<Layout title={`${toolName} - 工具箱 - Maxwell.Science`} description={toolDesc}>
  <section class="tool-page-hao">
    <KitToolWrapper title={toolName} description={toolDesc} client:load>
      <div class="hao-tool-content" id="tool-content">
                <div class="hao-tab-buttons" style="display:flex;gap:0.2rem;margin-bottom:0.4rem;flex-wrap:wrap;">
                  <button class="hao-press-btn--primary" data-tab="weibo">微博</button>
                  <button class="hao-press-btn--secondary" data-tab="zhihu">知乎</button>
                  <button class="hao-press-btn--secondary" data-tab="baidu">百度</button>
                  <button class="hao-press-btn--secondary" data-tab="douyin">抖音</button>
                </div>
                <div id="loading" style="display:none;text-align:center;padding:1rem;color:var(--color-cale-text);">加载中...</div>
                <div id="hot-list"></div>
                <div style="margin-top:0.4rem;text-align:center;">
                  <button id="refresh-btn" class="hao-press-btn hao-press-btn--secondary" style="font-size:0.22rem;">🔄 刷新</button>
                </div>
                <div style="margin-top:0.3rem;padding:0.3rem;background:var(--color-background-one);border-radius:15px;font-size:0.22rem;color:var(--color-tm-text);text-align:center;">
                  * 数据为模拟演示，实时热榜请访问各平台
                </div>
              </div>
    </KitToolWrapper>
  </section>
</Layout>

<style>
  .tool-page-hao {
    padding-top: calc(var(--header-height) + 2rem);
    padding-bottom: 4rem;
    min-height: 100vh;
  }
  @media (max-width: 767px) {
    .tool-page-hao { padding-top: calc(var(--header-height) + 1rem); }
  }
</style>

<script is:inline>
</script>
