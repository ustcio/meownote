---
import AppLayout from '@layouts/AppLayout.astro';

const title = 'Workspace Admin Login - Maxwell.Science';
const description = 'Secure access for the Maxwell workspace.';

const slides = [
  {
    kicker: 'Cowork',
    title: 'Cowork handles your',
    highlight: 'weekly reports',
    description: 'Automatically generate performance summaries and insights.',
    image: '/images/carousel/cowork-how-01.webp',
  },
  {
    kicker: 'Cowork',
    title: 'Cowork handles your',
    highlight: 'data analysis',
    description: 'Transform raw data into actionable intelligence.',
    image: '/images/carousel/cowork-how-02.webp',
  },
  {
    kicker: 'Cowork',
    title: 'Cowork handles your',
    highlight: 'team collaboration',
    description: 'Seamless workflows across your entire organization.',
    image: '/images/carousel/cowork-how-03.webp',
  },
  {
    kicker: 'Cowork',
    title: 'Cowork handles your',
    highlight: 'research tasks',
    description: 'Deep research capabilities at your fingertips.',
    image: '/images/carousel/blog-plugins.jpg',
  }
];
---

<AppLayout title={title} description={description}>
  <section class="workspace-login-page">
    <nav class="workspace-login-nav">
      <div class="workspace-login-nav__inner">
        <a href="/" class="workspace-login-brand" aria-label="Back to Maxwell home">
          <span class="workspace-login-brand__wordmark">Maxwell<span>.<span>Science</span></span></span>
        </a>

        <div class="workspace-login-nav__links" aria-label="Workspace navigation">
          <a href="/chatbot/">Chat</a>
          <a href="/workspace/">Workspace</a>
          <a href="/kit/">Kit</a>
          <a href="/about/">About</a>
        </div>

        <a href="/" class="workspace-login-nav__home">Back home</a>
      </div>
    </nav>

    <div class="workspace-login-shell">
      <!-- Left: Text + Login Card -->
      <div class="workspace-login-left">
        <div class="workspace-login-copy">
          <h1 class="workspace-login-title">
            Think fast,<br/>build faster
          </h1>
          <p class="workspace-login-subtitle">Brainstorm with Maxwell, execute locally with integrated workflows.</p>
        </div>

        <div class="workspace-login-card">
          <form id="workspace-login-form" class="workspace-login-form">
            <button type="button" class="workspace-google-btn">
              <img src="/claude-favicon.png" alt="" width="18" height="18" />
              Continue as YangHao
            </button>

            <div class="workspace-login-divider">
              <span>or</span>
            </div>

            <label class="workspace-field">
              <input type="password" id="workspace-admin-password" name="password" placeholder="Enter administrator password" required />
            </label>

            <button type="submit" class="workspace-submit-btn" id="workspace-login-submit">
              <span class="btn-label">Enter workspace</span>
              <span class="btn-spinner" aria-hidden="true"></span>
            </button>

            <p class="workspace-legal">
              By continuing, you confirm this area is reserved for the Maxwell workspace administrator.
            </p>
            <p class="workspace-login-error" id="workspace-login-error" hidden></p>
          </form>
        </div>

        <a href="/" class="workspace-download-btn">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor">
            <path d="M18.71 19.5c-.83 1.24-1.71 2.45-3.05 2.47-1.34.03-1.77-.79-3.29-.79-1.53 0-2 .77-3.27.82-1.31.05-2.3-1.32-3.14-2.53C4.25 17 2.94 12.45 4.7 9.39c.87-1.52 2.43-2.48 4.12-2.51 1.28-.02 2.5.87 3.29.87.78 0 2.26-1.07 3.8-.91.65.03 2.47.26 3.64 1.98-.09.06-2.17 1.28-2.15 3.81.03 3.02 2.65 4.03 2.68 4.04-.03.07-.42 1.44-1.38 2.83M13 3.5c.73-.83 1.94-1.46 2.94-1.5.13 1.17-.34 2.35-1.04 3.19-.69.85-1.83 1.51-2.95 1.42-.15-1.15.41-2.35 1.05-3.11z"/>
          </svg>
          Download desktop app
        </a>
      </div>

      <!-- Right: Carousel Card (fixed size) -->
      <div class="workspace-login-right">
        <div class="carousel-card">
          <div class="carousel-card__header">
            <div class="carousel-card__logo">
              <svg width="18" height="18" viewBox="0 0 28 28" fill="none">
                <path d="M14 0C6.268 0 0 6.268 0 14s6.268 14 14 14 14-6.268 14-14S21.732 0 14 0z" fill="#D97757"/>
                <path d="M14 4c-5.523 0-10 4.477-10 10s4.477 10 10 10 10-4.477 10-10S19.523 4 14 4z" fill="#fff" opacity="0.3"/>
              </svg>
              <span>Maxwell</span>
            </div>
            <span class="carousel-card__kicker">Cowork</span>
          </div>

          <div class="carousel-viewport">
            {slides.map((slide, index) => (
              <div class="carousel-slide" data-index={index}>
                <div class="slide-text">
                  <h2 class="slide-title">
                    {slide.title}
                    <span class="slide-highlight">{slide.highlight}</span>
                  </h2>
                  <p class="slide-description">{slide.description}</p>
                </div>
                <div class="slide-image-wrapper">
                  <img src={slide.image} alt={slide.highlight} loading="lazy" />
                </div>
              </div>
            ))}
          </div>

          <div class="carousel-footer">
            <div class="carousel-dots">
              {slides.map((_, index) => (
                <button class="carousel-dot" data-slide={index} aria-label={`Slide ${index + 1}`}>
                  <span class="dot-inner"></span>
                </button>
              ))}
            </div>
          </div>

          <div class="carousel-progress">
            <div class="progress-bar"></div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <script is:inline define:vars={{ API_BASE: 'https://api.ustc.dev' }}>
    (function() {
      const ADMIN_USERNAME = 'YangHao';
      const TOKEN_KEYS = ['meownote_auth_token', 'auth_token'];
      const USER_KEYS = ['meownote_user_data', 'user'];
      let isRedirecting = false;

      const form = document.getElementById('workspace-login-form');
      const passwordInput = document.getElementById('workspace-admin-password');
      const errorEl = document.getElementById('workspace-login-error');
      const submitBtn = document.getElementById('workspace-login-submit');

      function setError(message) {
        if (!errorEl) return;
        errorEl.hidden = !message;
        errorEl.textContent = message || '';
      }

      function setLoading(loading) {
        submitBtn?.classList.toggle('is-loading', loading);
        if (submitBtn instanceof HTMLButtonElement) submitBtn.disabled = loading;
      }

      function readJSON(key) {
        try {
          const raw = localStorage.getItem(key);
          return raw ? JSON.parse(raw) : null;
        } catch {
          return null;
        }
      }

      function writeJSON(key, value) {
        localStorage.setItem(key, JSON.stringify(value));
      }

      function getStoredToken() {
        return TOKEN_KEYS.map((key) => localStorage.getItem(key)).find(Boolean) || null;
      }

      function getStoredUser() {
        return USER_KEYS.map((key) => readJSON(key)).find(Boolean) || null;
      }

      function isSafeWorkspacePath(path) {
        return Boolean(path) && path.startsWith('/workspace') && !path.startsWith('/workspace/login');
      }

      function persistSession(token, user) {
        if (window.__meownoteAuth?.setSession) {
          window.__meownoteAuth.setSession(token, user);
          return;
        }

        const nextToken = token || getStoredToken();
        if (nextToken) {
          localStorage.setItem('meownote_auth_token', nextToken);
          localStorage.setItem('auth_token', nextToken);
        }
        writeJSON('meownote_user_data', user);
        writeJSON('user', user);
        window.dispatchEvent(new CustomEvent('auth:login', { detail: user }));
      }

      function isAdminUser(user) {
        const username = user?.username || user?.name || '';
        return username === ADMIN_USERNAME;
      }

      function redirectToWorkspace(path) {
        if (isRedirecting) return;
        isRedirecting = true;
        window.location.replace(isSafeWorkspacePath(path) ? path : '/workspace/');
      }

      function hydrateIfLoggedIn() {
        const redirectCount = parseInt(localStorage.getItem('meownote_redirect_count') || '0', 10);
        if (redirectCount > 3) {
          localStorage.removeItem('meownote_redirect_count');
          return;
        }

        const token = window.__meownoteAuth?.getToken?.() || getStoredToken();
        const user = window.__meownoteAuth?.getUser?.() || getStoredUser();

        if (token && isAdminUser(user)) {
          localStorage.setItem('meownote_redirect_count', String(redirectCount + 1));
          redirectToWorkspace('/workspace/');
        } else {
          localStorage.removeItem('meownote_redirect_count');
        }
      }

      async function attemptLogin(path, account, password) {
        const response = await fetch(`${API_BASE}${path}`, {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ username: account, password }),
        });
        const result = await response.json().catch(() => ({}));
        return { response, result };
      }

      document.querySelector('.workspace-google-btn')?.addEventListener('click', () => {
        passwordInput?.focus();
      });

      form?.addEventListener('submit', async (event) => {
        event.preventDefault();
        setError('');

        const password = passwordInput instanceof HTMLInputElement ? passwordInput.value.trim() : '';

        if (!password) {
          setError('请输入管理员密码。');
          return;
        }

        setLoading(true);

        try {
          const loginPaths = ['/api/admin/login', '/api/superadmin/login', '/api/trading/login'];
          let response = null;
          let result = {};

          for (const path of loginPaths) {
            const attempt = await attemptLogin(path, ADMIN_USERNAME, password);
            response = attempt.response;
            result = attempt.result;

            if (response.ok && result.success) {
              break;
            }
          }

          if (!response || !response.ok || !result.success) {
            throw new Error(result.message || result.error || '用户名或密码错误');
          }

          const user = result.user || {};
          if (!isAdminUser(user)) {
            throw new Error('当前账号没有 Workspace 超级管理员权限。');
          }

          persistSession(result.token, user);
          window.__meownoteToast?.show('Workspace access granted', 'success');
          redirectToWorkspace('/workspace/');
        } catch (error) {
          setError(error instanceof Error ? error.message : '登录失败，请稍后重试。');
          window.__meownoteToast?.show('Workspace login failed', 'error');
        } finally {
          setLoading(false);
        }
      });

      hydrateIfLoggedIn();

      // Carousel logic
      const carouselCard = document.querySelector('.carousel-card');
      if (carouselCard) {
        const slides = carouselCard.querySelectorAll('.carousel-slide');
        const dots = carouselCard.querySelectorAll('.carousel-dot');
        const progressBar = carouselCard.querySelector('.progress-bar');
        let currentIndex = 0;
        let autoplayInterval = null;
        const autoplayDelay = 5000;

        function goToSlide(index, direction = 'down') {
          if (index === currentIndex) return;
          const currentSlide = slides[currentIndex];
          const nextSlide = slides[index];
          currentSlide.classList.remove('active');
          currentSlide.classList.add(direction === 'down' ? 'exit-up' : '');
          nextSlide.style.transform = direction === 'down' ? 'translateY(1rem)' : 'translateY(-1rem)';
          void nextSlide.offsetHeight;
          nextSlide.classList.add('active');
          nextSlide.style.transform = '';
          dots[currentIndex].classList.remove('active');
          dots[index].classList.add('active');
          currentIndex = index;
          resetProgress();
        }

        function nextSlide() {
          goToSlide((currentIndex + 1) % slides.length, 'down');
        }

        function resetProgress() {
          if (progressBar) {
            progressBar.style.transition = 'none';
            progressBar.style.width = '0%';
            void progressBar.offsetHeight;
            progressBar.style.transition = `width ${autoplayDelay}ms linear`;
            progressBar.style.width = '100%';
          }
        }

        function startAutoplay() {
          stopAutoplay();
          resetProgress();
          autoplayInterval = setInterval(nextSlide, autoplayDelay);
        }

        function stopAutoplay() {
          if (autoplayInterval) {
            clearInterval(autoplayInterval);
            autoplayInterval = null;
          }
        }

        dots.forEach((dot, index) => {
          dot.addEventListener('click', () => {
            if (index === currentIndex) return;
            goToSlide(index, index > currentIndex ? 'down' : 'up');
            startAutoplay();
          });
        });

        carouselCard.addEventListener('mouseenter', stopAutoplay);
        carouselCard.addEventListener('mouseleave', startAutoplay);

        if (slides.length > 0) {
          slides[0].classList.add('active');
          if (dots.length > 0) dots[0].classList.add('active');
          startAutoplay();
        }
      }
    })();
  </script>
</AppLayout>

<style>
  .workspace-login-page {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    background: var(--bg-primary);
    color: #171412;
  }

  /* ── Nav ── */
  .workspace-login-nav {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: var(--z-fixed);
    background: var(--bg-primary);
  }

  .workspace-login-nav__inner {
    max-width: var(--container-xl);
    width: calc(100% - (2 * clamp(1.5rem, 1.07rem + 2.14vw, 3rem)));
    min-height: var(--header-height);
    margin: 0 auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1.5rem;
  }

  .workspace-login-brand {
    display: inline-flex;
    align-items: center;
    gap: 0;
    min-height: var(--header-height);
    padding: 0.1rem 0;
    color: inherit;
    text-decoration: none;
  }

  .workspace-login-brand__wordmark {
    font-family: var(--font-editorial);
    font-size: 1.55rem;
    font-weight: 500;
    letter-spacing: -0.03em;
    line-height: 1;
    white-space: nowrap;
    color: var(--text-primary);
  }

  .workspace-login-brand__wordmark > span {
    color: var(--color-primary);
  }

  .workspace-login-brand__wordmark > span > span {
    color: var(--text-secondary);
  }

  .workspace-login-nav__links {
    display: none;
    align-items: center;
    gap: 1.4rem;
    margin-left: auto;
  }

  @media (min-width: 960px) {
    .workspace-login-nav__links {
      display: flex;
    }
  }

  .workspace-login-nav__links a,
  .workspace-login-nav__home {
    color: var(--nav-link);
    text-decoration: none;
    font-size: 0.95rem;
    font-weight: var(--font-medium);
    transition: color var(--duration-fast) var(--ease-default);
  }

  .workspace-login-nav__links a:hover,
  .workspace-login-nav__home:hover {
    color: var(--nav-link-hover);
  }

  .workspace-login-nav__home {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 2.75rem;
    padding: 0.72rem 1rem;
    border-radius: var(--radius-full);
    border: 1px solid var(--border-primary);
    background: transparent;
    color: var(--nav-link);
    font-size: 0.95rem;
    font-weight: var(--font-medium);
  }

  .workspace-login-nav__home:hover {
    color: var(--nav-link-hover);
    background: var(--bg-secondary);
  }

  /* ── Shell ── */
  .workspace-login-shell {
    max-width: var(--container-xl);
    width: calc(100% - (2 * clamp(1.5rem, 1.07rem + 2.14vw, 3rem)));
    margin: 0 auto;
    padding-top: var(--header-height);
    padding-bottom: 0;
    min-height: calc(100vh - var(--header-height));
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  /* ── Left: Text + Login Card ── */
  .workspace-login-left {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 1.5rem;
    max-width: 32rem;
    padding-left: clamp(2rem, 4vw, 4rem);
  }

  .workspace-login-copy {
    margin-bottom: 0.5rem;
  }

  .workspace-login-title {
    font-family: var(--font-editorial);
    font-size: clamp(3rem, 6vw, 4.8rem);
    font-weight: 500;
    line-height: 1.1;
    letter-spacing: -0.02em;
    color: var(--text-primary);
    margin: 0 0 1.2rem;
  }

  .workspace-login-subtitle {
    font-size: clamp(1.1rem, 2vw, 1.3rem);
    line-height: 1.5;
    color: var(--text-secondary);
    margin: 0;
    max-width: 32rem;
  }

  .workspace-login-card {
    width: 100%;
    padding: 1.5rem;
    border-radius: 1.25rem;
    background: #fff;
    border: 1px solid rgba(108, 87, 73, 0.1);
    box-shadow: 
      0 4px 6px rgba(94, 66, 41, 0.02),
      0 12px 40px rgba(94, 66, 41, 0.08);
    transition: box-shadow 0.25s ease;
  }

  .workspace-login-card:hover {
    box-shadow: 
      0 4px 6px rgba(94, 66, 41, 0.02),
      0 16px 48px rgba(94, 66, 41, 0.12);
  }

  .workspace-login-form {
    display: grid;
    gap: 0.75rem;
  }

  .workspace-google-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.6rem;
    min-height: 2.75rem;
    width: 100%;
    border-radius: 0.65rem;
    border: 1px solid rgba(94, 72, 60, 0.12);
    background: #fff;
    color: #171412;
    font-size: 0.85rem;
    font-weight: 500;
    cursor: pointer;
    transition: background var(--duration-fast) var(--ease-default);
  }

  .workspace-google-btn:hover {
    background: #faf8f5;
  }

  .workspace-login-divider {
    display: flex;
    align-items: center;
    justify-content: center;
    color: #9e948c;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    font-size: 0.7rem;
    font-weight: 500;
  }

  .workspace-field {
    display: grid;
  }

  .workspace-field input {
    min-height: 2.75rem;
    border-radius: 0.65rem;
    border: 1px solid rgba(80, 63, 52, 0.12);
    background: #faf8f5;
    padding: 0 0.85rem;
    font-size: 0.85rem;
    color: #171412;
    outline: none;
    transition: border-color var(--duration-fast) var(--ease-default);
  }

  .workspace-field input::placeholder {
    color: #9e948c;
  }

  .workspace-field input:focus {
    border-color: var(--color-primary);
  }

  .workspace-submit-btn {
    position: relative;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 2.75rem;
    width: 100%;
    border: none;
    border-radius: 0.65rem;
    background: #171412;
    color: #fff;
    font-size: 0.85rem;
    font-weight: 500;
    cursor: pointer;
    transition: background var(--duration-fast) var(--ease-default);
  }

  .workspace-submit-btn:hover {
    background: #28211d;
  }

  .workspace-submit-btn .btn-spinner {
    position: absolute;
    width: 16px;
    height: 16px;
    border: 2px solid rgba(255, 255, 255, 0.3);
    border-top-color: #fff;
    border-radius: 50%;
    opacity: 0;
    animation: workspace-spin 0.8s linear infinite;
  }

  .workspace-submit-btn.is-loading .btn-label {
    opacity: 0;
  }

  .workspace-submit-btn.is-loading .btn-spinner {
    opacity: 1;
  }

  .workspace-legal {
    margin: 0;
    font-size: 0.72rem;
    color: #9e948c;
    line-height: 1.5;
    text-align: center;
  }

  .workspace-legal a {
    color: #9e948c;
    text-decoration: underline;
    text-underline-offset: 2px;
  }

  .workspace-legal a:hover {
    color: #6a625b;
  }

  .workspace-login-error {
    margin: 0;
    min-height: 1rem;
    font-size: 0.8rem;
    color: var(--color-danger);
    text-align: center;
  }

  .workspace-download-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    min-height: 2.5rem;
    padding: 0.5rem 1rem;
    border-radius: 0.5rem;
    border: 1px solid rgba(108, 87, 73, 0.15);
    background: transparent;
    color: var(--text-secondary);
    font-size: 0.82rem;
    font-weight: 500;
    text-decoration: none;
    transition: all var(--duration-fast) var(--ease-default);
  }

  .workspace-download-btn:hover {
    background: var(--bg-secondary);
    color: var(--text-primary);
  }

  .workspace-download-btn svg {
    flex-shrink: 0;
  }

  /* ── Right: Carousel Card ── */
  .workspace-login-right {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .carousel-card {
    position: relative;
    width: 100%;
    max-width: 34rem;
    height: 38rem;
    padding: 2.5rem;
    border-radius: 1.5rem;
    background: var(--bg-primary);
    border: 1px solid rgba(108, 87, 73, 0.1);
    box-shadow: 
      0 4px 6px rgba(94, 66, 41, 0.02),
      0 12px 40px rgba(94, 66, 41, 0.08);
    overflow: hidden;
    display: flex;
    flex-direction: column;
    transition: box-shadow 0.25s ease;
  }

  .carousel-card:hover {
    box-shadow: 
      0 4px 6px rgba(94, 66, 41, 0.02),
      0 16px 48px rgba(94, 66, 41, 0.12);
  }

  .carousel-card__header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 1rem;
    flex-shrink: 0;
  }

  .carousel-card__logo {
    display: flex;
    align-items: center;
    gap: 0.45rem;
    font-family: var(--font-editorial);
    font-size: 0.95rem;
    font-weight: 500;
    color: #1f1b18;
  }

  .carousel-card__logo svg {
    flex-shrink: 0;
  }

  .carousel-card__kicker {
    display: inline-flex;
    align-items: center;
    padding: 0.2rem 0.55rem;
    border-radius: 999px;
    background: rgba(217, 119, 87, 0.08);
    color: var(--color-primary);
    font-size: 0.6rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  /* Fixed viewport */
  .carousel-viewport {
    position: relative;
    flex: 1;
    min-height: 0;
    overflow: hidden;
  }

  .carousel-slide {
    position: absolute;
    inset: 0;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    opacity: 0;
    transform: translateY(1rem);
    transition:
      opacity 0.5s cubic-bezier(0.2, 0.7, 0.2, 1),
      transform 0.5s cubic-bezier(0.2, 0.7, 0.2, 1);
    pointer-events: none;
  }

  .carousel-slide.active {
    opacity: 1;
    transform: translateY(0);
    pointer-events: auto;
    position: relative;
  }

  .carousel-slide.exit-up {
    opacity: 0;
    transform: translateY(-1rem);
  }

  .slide-text {
    flex-shrink: 0;
  }

  .slide-title {
    font-family: var(--font-editorial);
    font-size: 1.25rem;
    font-weight: 400;
    line-height: 1.2;
    letter-spacing: -0.03em;
    color: #1f1b18;
    margin: 0 0 0.25rem;
  }

  .slide-highlight {
    display: block;
    color: var(--color-primary);
    font-weight: 500;
  }

  .slide-description {
    font-size: 0.82rem;
    line-height: 1.5;
    color: #6a625b;
    margin: 0;
  }

  .slide-image-wrapper {
    flex: 1;
    min-height: 0;
    border-radius: 0.75rem;
    overflow: hidden;
    background: rgba(217, 119, 87, 0.04);
    border: 1px solid rgba(108, 87, 73, 0.06);
  }

  .slide-image-wrapper img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  /* Footer */
  .carousel-footer {
    flex-shrink: 0;
    padding-top: 1rem;
  }

  .carousel-dots {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.4rem;
  }

  .carousel-dot {
    position: relative;
    width: 2rem;
    height: 0.25rem;
    padding: 0;
    border: none;
    background: transparent;
    cursor: pointer;
    border-radius: 999px;
    overflow: hidden;
  }

  .dot-inner {
    position: absolute;
    inset: 0;
    background: rgba(108, 87, 73, 0.15);
    border-radius: 999px;
    transition: background 0.3s ease;
  }

  .carousel-dot.active .dot-inner {
    background: var(--color-primary);
  }

  .carousel-dot:hover .dot-inner {
    background: rgba(108, 87, 73, 0.3);
  }

  .carousel-dot.active:hover .dot-inner {
    background: var(--color-primary);
  }

  /* Progress bar */
  .carousel-progress {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 2px;
    background: rgba(108, 87, 73, 0.06);
  }

  .progress-bar {
    height: 100%;
    width: 0%;
    background: var(--color-primary);
    transition: width 0.1s linear;
  }

  @keyframes workspace-spin {
    to { transform: rotate(360deg); }
  }

  /* ── Responsive ── */
  @media (max-width: 1100px) {
    .workspace-login-shell {
      gap: 3rem;
    }
  }

  @media (max-width: 900px) {
    .workspace-login-nav__links {
      display: none;
    }

    .workspace-login-shell {
      grid-template-columns: 1fr;
      max-width: 32rem;
      gap: 2rem;
    }

    .workspace-login-right {
      display: none;
    }

    .workspace-login-left {
      max-width: 100%;
      align-items: center;
      text-align: center;
      padding-left: 0;
    }

    .workspace-login-copy {
      margin-bottom: 1rem;
    }

    .workspace-login-page {
      padding: 1rem;
    }
  }

  @media (max-width: 480px) {
    .workspace-login-card {
      padding: 1.25rem;
      border-radius: 1rem;
    }

    .workspace-login-title {
      font-size: clamp(2.2rem, 8vw, 3rem);
    }
  }
</style>
