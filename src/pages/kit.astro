---
import Layout from '@layouts/Layout.astro';
import { toolCategories } from '@data/tool-categories';

const pageTitle = '工具箱';
const pageDesc = '在线工具集合 - 日常应用、文字处理、图片处理、编程工具等';
---

<Layout title={`${pageTitle} - Maxwell.Science`} description={pageDesc}>
  <section class="kit-page">
    <div class="kit-container">
      <div class="kit-main-layout">
        <aside class="kit-sidebar" id="kit-sidebar">
          <div class="kit-sidebar-logo">
            <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8">
              <path d="M14.7 6.3a1 1 0 0 0 0 1.4l1.6 1.6a1 1 0 0 0 1.4 0l3.77-3.77a6 6 0 0 1-7.94 7.94l-6.91 6.91a2.12 2.12 0 0 1-3-3l6.91-6.91a6 6 0 0 1 7.94-7.94l-3.76 3.76z"/>
            </svg>
            <span class="kit-sidebar-title">工具箱</span>
          </div>
          <nav class="kit-sidebar-nav">
            {toolCategories.map((cat) => (
              <a
                href={`#${cat.id}`}
                class="kit-sidebar-item"
                data-sidebar-category={cat.id}
              >
                <span class="kit-sidebar-icon">{cat.icon}</span>
                <span class="kit-sidebar-text">{cat.name}</span>
              </a>
            ))}
          </nav>
        </aside>

        <div class="kit-main">
          <div class="kit-search-bar">
            <svg class="kit-search-icon" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <circle cx="11" cy="11" r="8"></circle>
              <line x1="21" y1="21" x2="16.65" y2="16.65"></line>
            </svg>
            <input type="text" id="kit-search-input" class="kit-search-input" placeholder="搜索工具..." />
            <button class="kit-search-clear" id="search-clear" style="display:none;">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <line x1="18" y1="6" x2="6" y2="18"></line>
                <line x1="6" y1="6" x2="18" y2="18"></line>
              </svg>
            </button>
          </div>

          <div class="kit-content">
            {toolCategories.map(cat => (
              <section class="kit-section" id={cat.id} data-category>
                <div class="kit-section-header">
                  <h2 class="kit-section-title">
                    <span class="kit-section-emoji">{cat.icon}</span>
                    {cat.name}
                  </h2>
                  <span class="kit-section-count">{cat.tools.length} 个工具</span>
                </div>
                <div class="kit-tool-grid">
                  {cat.tools.map(tool => (
                    <a href={tool.path} class="kit-tool-card" data-tool-id={tool.id} data-tool-name={tool.name} data-tool-desc={tool.desc}>
                      <div class="kit-tool-card-icon">
                        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6">
                          {tool.id === 'hot-list' && <><path d="M12 20V10"/><path d="M18 20V4"/><path d="M6 20v-4"/></>}
                          {tool.id === 'news' && <><path d="M4 22h16a2 2 0 0 0 2-2V4a2 2 0 0 0-2-2H8a2 2 0 0 0-2 2v16a2 2 0 0 1-2 2Zm0 0a2 2 0 0 1-2-2v-9c0-1.1.9-2 2-2h2"/><path d="M18 14h-8"/><path d="M15 18h-5"/><path d="M10 6h8v4h-8V6Z"/></>}
                          {tool.id === 'qrcode' && <><rect width="5" height="5" x="3" y="3" rx="1"/><rect width="5" height="5" x="16" y="3" rx="1"/><rect width="5" height="5" x="3" y="16" rx="1"/><path d="M21 16h-3a2 2 0 0 0-2 2v3"/><path d="M21 21v.01"/><path d="M12 7v3a2 2 0 0 1-2 2H7"/><path d="M3 12h.01"/><path d="M12 3h.01"/><path d="M12 16v.01"/></>}
                          {tool.id !== 'hot-list' && tool.id !== 'news' && tool.id !== 'qrcode' && <><path d="M14.7 6.3a1 1 0 0 0 0 1.4l1.6 1.6a1 1 0 0 0 1.4 0l3.77-3.77a6 6 0 0 1-7.94 7.94l-6.91 6.91a2.12 2.12 0 0 1-3-3l6.91-6.91a6 6 0 0 1 7.94-7.94l-3.76 3.76z"/></>}
                        </svg>
                      </div>
                      <div class="kit-tool-card-body">
                        <h3 class="kit-tool-card-title">{tool.name}</h3>
                        <p class="kit-tool-card-desc">{tool.desc}</p>
                      </div>
                      {tool.status === 'todo' && (
                        <span class="kit-tool-badge kit-tool-badge-todo">开发中</span>
                      )}
                    </a>
                  ))}
                </div>
              </section>
            ))}
          </div>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  .kit-page {
    min-height: 100vh;
    background: var(--bg-primary);
    padding-top: var(--space-4);
  }

  .kit-container {
    max-width: var(--container-2xl);
    margin: 0 auto;
    padding: 0 var(--space-6);
  }

  .kit-main-layout {
    display: flex;
    gap: var(--space-1);
    align-items: flex-start;
  }

  /* ===== Sidebar ===== */
  .kit-sidebar {
    width: 200px;
    min-width: 200px;
    flex-shrink: 0;
    position: sticky;
    top: calc(var(--header-total-height) + var(--space-4));
    max-height: calc(100vh - var(--header-total-height) - var(--space-8));
    overflow-y: auto;
    padding: 0 12px;
    scrollbar-width: none;
  }

  .kit-sidebar::-webkit-scrollbar {
    display: none;
  }

  .kit-sidebar-logo {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 8px 16px 16px;
    margin-bottom: 4px;
  }

  .kit-sidebar-logo svg {
    width: 32px;
    height: 32px;
    color: var(--color-primary);
  }

  .kit-sidebar-title {
    font-size: 18px;
    font-weight: 600;
    color: var(--text-primary);
    letter-spacing: -0.01em;
  }

  .kit-sidebar-nav {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .kit-sidebar-item {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 8px 12px;
    border-radius: 20px;
    font-size: 14px;
    color: var(--text-secondary);
    text-decoration: none;
    transition: all 0.3s ease;
  }

  .kit-sidebar-item:first-child {
    margin-top: 8px;
  }

  .kit-sidebar-item:hover {
    background-color: var(--bg-secondary);
    color: var(--text-primary);
  }

  .kit-sidebar-item.active {
    background-color: var(--bg-tertiary);
    color: var(--text-primary);
    font-weight: 500;
  }

  .kit-sidebar-icon {
    font-size: 18px;
    line-height: 1;
    flex-shrink: 0;
    width: 24px;
    height: 24px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .kit-sidebar-text {
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  /* ===== Main Content ===== */
  .kit-main {
    flex: 1;
    min-width: 0;
    display: flex;
    flex-direction: column;
  }

  /* ===== Search Bar ===== */
  .kit-search-bar {
    display: flex;
    align-items: center;
    gap: 8px;
    background: var(--bg-secondary);
    border-radius: 20px;
    padding: 8px 16px;
    margin-bottom: 24px;
    max-width: 360px;
    transition: background-color 0.2s ease, box-shadow 0.2s ease;
  }

  .kit-search-bar:focus-within {
    background: var(--bg-tertiary);
    box-shadow: 0 0 0 2px var(--color-primary-alpha-20);
  }

  .kit-search-icon {
    flex-shrink: 0;
    color: var(--text-tertiary);
    width: 18px;
    height: 18px;
  }

  .kit-search-input {
    flex: 1;
    background: transparent;
    border: none;
    outline: none;
    font-size: 14px;
    color: var(--text-primary);
    font-family: var(--font-body);
  }

  .kit-search-input::placeholder {
    color: var(--text-quaternary);
  }

  .kit-search-clear {
    display: flex;
    align-items: center;
    justify-content: center;
    background: none;
    border: none;
    padding: 2px;
    cursor: pointer;
    color: var(--text-tertiary);
    border-radius: 50%;
    transition: color 0.2s ease;
  }

  .kit-search-clear:hover {
    color: var(--text-primary);
  }

  /* ===== Content ===== */
  .kit-content {
    display: flex;
    flex-direction: column;
    gap: 32px;
    padding-bottom: 60px;
  }

  /* ===== Section ===== */
  .kit-section-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 16px;
  }

  .kit-section-title {
    font-size: 18px;
    font-weight: 500;
    color: var(--text-primary);
    display: flex;
    align-items: center;
    gap: 8px;
    letter-spacing: -0.01em;
  }

  .kit-section-emoji {
    font-size: 20px;
    line-height: 1;
  }

  .kit-section-count {
    font-size: 12px;
    color: var(--text-quaternary);
    font-weight: 400;
  }

  /* ===== Tool Grid ===== */
  .kit-tool-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
    gap: 16px;
  }

  /* ===== Tool Card ===== */
  .kit-tool-card {
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    justify-content: flex-start;
    background-color: var(--card-bg);
    padding: 16px;
    border-radius: 12px;
    border: 1px solid var(--card-border);
    text-decoration: none;
    transition: transform 0.3s ease, box-shadow 0.3s ease, border-color 0.3s ease;
    cursor: pointer;
    min-height: 110px;
  }

  .kit-tool-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.06);
    border-color: var(--card-border-hover);
  }

  .kit-tool-card.hidden {
    display: none;
  }

  .kit-tool-card-icon {
    width: 28px;
    height: 28px;
    margin-bottom: 10px;
    color: var(--text-tertiary);
    display: flex;
    align-items: center;
    justify-content: center;
    transition: color 0.2s ease;
  }

  .kit-tool-card:hover .kit-tool-card-icon {
    color: var(--color-primary);
  }

  .kit-tool-card-icon svg {
    width: 22px;
    height: 22px;
  }

  .kit-tool-card-body {
    flex: 1;
    min-width: 0;
  }

  .kit-tool-card-title {
    font-size: 15px;
    font-weight: 500;
    color: var(--text-primary);
    margin-bottom: 4px;
    line-height: 1.3;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .kit-tool-card-desc {
    font-size: 13px;
    color: var(--text-tertiary);
    line-height: 1.4;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .kit-tool-badge {
    position: absolute;
    top: 12px;
    right: 12px;
    font-size: 10px;
    font-weight: 500;
    padding: 2px 8px;
    border-radius: 10px;
    white-space: nowrap;
    letter-spacing: 0.02em;
  }

  .kit-tool-badge-todo {
    background: var(--bg-tertiary);
    color: var(--text-quaternary);
  }

  /* ===== Responsive ===== */
  @media (max-width: 900px) {
    .kit-sidebar {
      display: none;
    }

    .kit-main-layout {
      gap: 0;
    }

    .kit-container {
      padding: 0 16px;
    }

    .kit-search-bar {
      max-width: 100%;
    }
  }

  @media (max-width: 600px) {
    .kit-page {
      padding-top: var(--space-2);
    }

    .kit-tool-grid {
      grid-template-columns: 1fr;
      gap: 0;
    }

    .kit-tool-card {
      flex-direction: row;
      align-items: center;
      min-height: auto;
      padding: 14px 16px;
      border-radius: 0;
      border: none;
      border-bottom: 1px solid var(--border-secondary);
      background: transparent;
    }

    .kit-tool-card:hover {
      transform: none;
      box-shadow: none;
      background-color: var(--bg-secondary);
    }

    .kit-tool-card-icon {
      width: 24px;
      height: 24px;
      margin-bottom: 0;
      margin-right: 14px;
      opacity: 0.8;
      flex-shrink: 0;
    }

    .kit-tool-card-title {
      font-size: 15px;
      font-weight: 400;
    }

    .kit-tool-card-desc {
      display: none;
    }

    .kit-tool-badge-todo {
      background: var(--bg-tertiary);
    }

    .kit-section-title {
      font-size: 16px;
    }

    .kit-content {
      gap: 24px;
    }
  }
</style>

<script>
  const searchInput = document.getElementById('kit-search-input') as HTMLInputElement | null;
  const searchClear = document.getElementById('search-clear') as HTMLButtonElement | null;
  const toolCards = document.querySelectorAll('.kit-tool-card');
  const sections = document.querySelectorAll('[data-category]');
  const sidebarItems = document.querySelectorAll('.kit-sidebar-item');

  searchInput?.addEventListener('input', () => {
    const query = searchInput.value.toLowerCase().trim();
    if (searchClear) {
      searchClear.style.display = query ? 'flex' : 'none';
    }
    if (!query) {
      toolCards.forEach(card => card.classList.remove('hidden'));
      sections.forEach(section => section.style.display = '');
      return;
    }
    sections.forEach(section => {
      const sectionCards = section.querySelectorAll('.kit-tool-card');
      let hasVisible = false;
      sectionCards.forEach(card => {
        const name = (card.dataset.toolName || '').toLowerCase();
        const desc = (card.dataset.toolDesc || '').toLowerCase();
        const matches = name.includes(query) || desc.includes(query);
        card.classList.toggle('hidden', !matches);
        if (matches) hasVisible = true;
      });
      (section as HTMLElement).style.display = hasVisible ? '' : 'none';
    });
  });

  searchClear?.addEventListener('click', () => {
    if (searchInput) {
      searchInput.value = '';
      searchInput.dispatchEvent(new Event('input'));
      searchInput.focus();
    }
  });

  const sectionEls = Array.from(sections) as HTMLElement[];
  const sidebarItemEls = Array.from(sidebarItems) as HTMLAnchorElement[];

  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          const id = entry.target.id;
          sidebarItemEls.forEach(item => {
            item.classList.toggle('active', item.dataset.sidebarCategory === id);
          });
        }
      });
    },
    {
      rootMargin: '-80px 0px -60% 0px',
      threshold: 0,
    }
  );

  sectionEls.forEach(section => observer.observe(section));

  sidebarItemEls.forEach(item => {
    item.addEventListener('click', (e) => {
      e.preventDefault();
      const targetId = item.getAttribute('href')?.replace('#', '');
      const target = targetId ? document.getElementById(targetId) : null;
      if (target) {
        const offset = 80;
        const top = target.getBoundingClientRect().top + window.scrollY - offset;
        window.scrollTo({ top, behavior: 'smooth' });
      }
    });
  });
</script>
