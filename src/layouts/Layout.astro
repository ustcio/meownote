---
import SiteLayout from './SiteLayout.astro';

interface Props {
  title: string;
  description?: string;
  image?: string;
  showAuthModal?: boolean;
}

const { title, description, image, showAuthModal = true } = Astro.props;
---

<SiteLayout title={title} description={description} image={image} showAuthModal={showAuthModal}>
  <slot />
</SiteLayout>
