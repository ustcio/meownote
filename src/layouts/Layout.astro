---
import SiteLayout from './SiteLayout.astro';

interface Props {
  title: string;
  description?: string;
  image?: string;
  showAuthModal?: boolean;
  showHeader?: boolean;
  showFooter?: boolean;
}

const { title, description, image, showAuthModal = true, showHeader = true, showFooter = true } = Astro.props;
---

<SiteLayout title={title} description={description} image={image} showAuthModal={showAuthModal} showHeader={showHeader} showFooter={showFooter}>
  <slot />
</SiteLayout>
